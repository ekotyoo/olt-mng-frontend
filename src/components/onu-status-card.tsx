import { Card, CardContent, CardHeader, CardTitle } from "./ui/card";

export default function OnuStatusCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ONU Status</CardTitle>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  );
}
